import React from "react";
import styled from "styled-components";

const StyledChatArea = styled.div`
  background-color: var(--wa-light-gray);
`;

export default function ChatArea() {
  return <StyledChatArea></StyledChatArea>;
}
